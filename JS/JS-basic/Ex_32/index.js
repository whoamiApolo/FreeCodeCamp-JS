let xyz = "Jello World"
console.log(xyz)

/* 
xyz[0] = "HelloWorld" 
- String são imutáveis, o que significa que elas não podem ser alteradas após serem criadas.
Por isso, devemos alterá-la atribuindo a ela uma nova string, dessa forma:
*/

xyz = "HelloWorld"
console.log(xyz)